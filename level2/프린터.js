function solution(priorities, location) {
	// var answer = 0;
	// var queue = priorities.shift();
	// var max = Math.max.apply(null, priorities);
}
// solution([2, 1, 3, 2], 2);
solution([1, 1, 9, 1, 1, 1], 0);
