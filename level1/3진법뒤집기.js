function solution(n) {
	var sam = n.toString(3); // 1200
	var trans = []

	console.log(trans);

	// return answer;
}
solution(45);

1. n을 삼진법 (sam) 으로 바꾸기
2. var trans = sam 을 쪼개서 순서를 바꿔주기
3. trans 를 10진법으로 표현하기
